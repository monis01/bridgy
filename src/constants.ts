// =============================================================================
// BRIDGY CONSTANTS
// =============================================================================

export const DEFAULTS = {
  TIMEOUT: 10000,
  MODE: 'duplex',
  DEBUG: false,
  AUTO_CONNECT: true,
  RETRIES: 5,
  RETRY_INTERVAL: 2000,
} as const;
